<h1>Scegliere se inviare avviso del seminario</h1>

<p class="alert alert-info">
Nella pagina successiva sarà possibile compilare la richiesta di <strong>compenso</strong> e/o <strong>rimborso</strong>.
</p>

<%= horizontal_form_for :seminar_mail, url: submit_mail_text_seminar_path(@seminar) do |f| %>  
  <%= f.input :to, label: "Destinatari", input_html: { value: "dipmat-seminari@unibo.it" }, 
                   hint: "dipmat-seminari@unibo.it comprende docenti, dottorandi, borsisti e coloro che fanno richiesta per essere inseriti. Inserire eventuali altri destinatari separati da virgola." %>  

  <%= f.input :subject, label: "Oggetto", input_html: { value: "AVVISO DI SEMINARIO" } %>

  <%= f.input :text, as: 'text', label: 'Testo', input_html: { value: "
    #{l @seminar.date, format: :seminar} 
    #{@seminar.where_to_s(visible: true)}

    #{@seminar.speaker}

    terrà un seminario dal titolo

    #{@seminar.title}

    organizzato da: #{@seminar.committee}

    https://seminari.dm.unibo.it/mat
    ", rows: "20", cols: "20" }  %>

    <%= f.submit "Invia avviso via e-mail" %> -
    <%= link_to "Salta invio e-mail", seminar_path(@seminar) %>  
<% end %>
