<%= dm_form_for Document.new, url: seminar_documents_path(@seminar), title: big_icon('paperclip', text: 'Allegati').html_safe, html: { multipart: true } do |document| %>
  <div class="alert alert-info">
    Questi allegati (per esempo locandine, slides, etc.) sono <strong>pubblici</strong>. 
    Nel caso di un rimborso/compenso dovrete inserire il curriculum vitae del relatore (in questo caso visibile solo all'amministrazione) <strong>in un altro form</strong>.
  </div>
  <% if @seminar.documents.any? %>  
    <ul>
      <% @seminar.documents.each do |document| %>
        <li>
          <%= icon('download') + "&nbsp;".html_safe + link_to(document.description, url_for(document.attach)) %> - 
          <%= link_to_delete seminar_document_path(@seminar, document) %>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p>Non sono ancora stati inseriti allegati a questo seminario.</p>
  <% end %>

  <%= document.input :description %>
  <%= document.input :attach, as: :file, class: "" %>
  <%= document.submit 'Invia' %> -
  <%= link_to 'indietro', root_path %>   
<% end %>
