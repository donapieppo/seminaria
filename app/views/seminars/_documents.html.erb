<h2>Allegati</h2>

<div class="attachs">
  <p>
  Questi allegati (per esempo locandine, slides, etc.) sono <strong>pubblici</strong>. 
  Nel caso di un rimborso/compenso dovrete inserire il curriculum vitae del relatore (in questo caso visibile solo all'amministrazione) in un altro form.
  </p>
  <% if @documents.any? %>  
    <ul>
      <% @documents.each do |document| %>
        <li><%= icon('download') + "&nbsp;".html_safe + link_to(document.description, url_for(document.attach)) %> - <%= link_to_delete document_path(document) %></li>
      <% end %>
    </ul>
  <% else %>
    <p>Non sono ancora stati inseriti allegati a questo seminario.</p>
  <% end %>

  <%= dm_form_for @document, url: seminar_documents_path(@seminar), html: { multipart: true } do |document| %>
    <fieldset>
      <%= document.input :description %>
      <%= document.input :attach, as: :file, class: "" %>
      <%= document.submit 'Invia' %> -
      <%= link_to 'indietro', root_path %>   
    </fieldset>
  <% end %>
</div>

