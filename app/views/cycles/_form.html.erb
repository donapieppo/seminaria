<%= dm_form_for @cycle do |cycle| %>  
  <%= cycle.input :title %>
  <%= cycle.input :description, input_html: { cols: 50, rows: 10 } %>
  <%= cycle.input :committee %>  
  <%= cycle.submit %>  
<% end %>


