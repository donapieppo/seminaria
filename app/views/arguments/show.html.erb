<h1>Seminari relativi a <%= @argument %> <%= link_to_edit("", edit_argument_path(@argument)) if policy(@argument).edit? %></h1>

<% @seminars.each do |seminar| %>  
  <%= render Seminar::SeminarComponent.new(seminar, current_user) %>  
<% end %>

