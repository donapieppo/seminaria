<% if policy(current_organization).see? %>  
  <div style="text-align: right">
    <% if ok_to_send %>  
      <div class="my-2">
      <%= link_to icon('print') + ' stampa lettera',  print_letter_repayment_path(repayment, format: :docx),   class: 'btn btn-sm btn-outline-primary' %> -
      <%= link_to icon('print') + ' stampa decreto',  print_decree_repayment_path(repayment, format: :docx),   class: 'btn btn-sm btn-outline-primary' %> - 
      <%= link_to icon('print') + ' stampa proposta', print_proposal_repayment_path(repayment, format: :docx), class: 'btn btn-sm btn-outline-primary' %>  
      </div>
      <div>
      <% if repayment.payment %>
        <%= link_to icon('print') + ' stampa pagamento',  print_repayment_repayment_path(repayment, format: :docx), class: 'btn btn-sm btn-outline-primary' %> - 
      <% end %>
      <% if repayment.refund %>  
        <%= link_to icon('print') + ' stampa nota spese', print_refund_repayment_path(repayment, format: :docx),    class: 'btn btn-sm btn-outline-primary' %> -  
      <% end %>
      <%= link_to icon('print') + ' stampa altri incarichi', print_other_repayment_path(repayment, format: :docx),    class: 'btn btn-sm btn-outline-primary' %>
      </div>
    <% else %>
      I documenti saranno disponibili solo dopo che tutti i dati richiesti saranno compilati.
    <% end %>
  </div>
<% end %>

